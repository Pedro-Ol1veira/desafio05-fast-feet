services:
  postgres:
    container_name: pg
    image: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: desafio05
      PGDATA: /data/postgres
    volumes:
      - ./data/pg:/data/postgres